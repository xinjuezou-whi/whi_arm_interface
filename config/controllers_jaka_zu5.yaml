whi_arm_interface:
  joints: &robot_joints
    - joint_1
    - joint_2
    - joint_3
    - joint_4
    - joint_5
    - joint_6

  controllers:
    state:
      type: joint_state_controller/JointStateController
      publish_rate: 50 # 50

    pos_controller:
      type: position_controllers/JointTrajectoryController
      joints: *robot_joints
      constraints:
        # if zero, the executing duration would be decided by the start and end time of planned trajectory
        # set it big enough especially for position controller
        goal_time: 1.0 # 0.5 sec
        stopped_velocity_tolerance: 0.05 # 0.01 rad/s
        joint_1: { trajectory: 0.2, goal: 0.1 }
        joint_2: { trajectory: 0.2, goal: 0.1 }
        joint_3: { trajectory: 0.2, goal: 0.1 }
        joint_4: { trajectory: 0.2, goal: 0.1 }
        joint_5: { trajectory: 0.2, goal: 0.1 }
        joint_6: { trajectory: 0.2, goal: 0.1 }
      # when starting the controller or canceling a trajectory, position hold mode is entered
      # this parameter specifies the time it takes to bring the current state (position and velocity) to a stop
      # if zero, the controller stops at the actual position
      stop_trajectory_duration: 0.0 # sec
      action_monitor_rate: 20

    scaled_pos_controller:
      type: position_controllers/ScaledJointTrajectoryController
      joints: *robot_joints
      constraints:
        # if zero, the executing duration would be decided by the start and end time of planned trajectory
        # set it big enough especially for position controller
        goal_time: 2.0 # 0.5 sec
        stopped_velocity_tolerance: 0.025 # 0.01 rad/s
        joint_1: { trajectory: 0.2, goal: 0.1 }
        joint_2: { trajectory: 0.2, goal: 0.1 }
        joint_3: { trajectory: 0.2, goal: 0.1 }
        joint_4: { trajectory: 0.2, goal: 0.1 }
        joint_5: { trajectory: 0.2, goal: 0.1 }
        joint_6: { trajectory: 0.2, goal: 0.1 }
      gains: # Required if controlling a velocity interface
        joint_1: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_2: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_3: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_4: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_5: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_6: {p: 100,  d: 1, i: 1, i_clamp: 1}
      # when starting the controller or canceling a trajectory, position hold mode is entered
      # this parameter specifies the time it takes to bring the current state (position and velocity) to a stop
      # if zero, the controller stops at the actual position
      stop_trajectory_duration: 1.0 # sec
      action_monitor_rate: 20

    vel_controller:
      type: velocity_controllers/JointTrajectoryController
      joints: *robot_joints
      constraints:
        # if zero, the executing duration would be decided by the start and end time of planned trajectory
        # set it big enough especially for position controller
        goal_time: 1.0 # 0.5 sec
        stopped_velocity_tolerance: 0.05 # 0.01 rad/s
        joint_1: { trajectory: 0.2, goal: 0.1 }
        joint_2: { trajectory: 0.2, goal: 0.1 }
        joint_3: { trajectory: 0.2, goal: 0.1 }
        joint_4: { trajectory: 0.2, goal: 0.1 }
        joint_5: { trajectory: 0.2, goal: 0.1 }
        joint_6: { trajectory: 0.2, goal: 0.1 }
      gains: # Required if controlling a velocity interface
        joint_1: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_2: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_3: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_4: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_5: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_6: {p: 100,  d: 1, i: 1, i_clamp: 1}
      # when starting the controller or canceling a trajectory, position hold mode is entered
      # this parameter specifies the time it takes to bring the current state (position and velocity) to a stop
      # if zero, the controller stops at the actual position
      stop_trajectory_duration: 0.0 # sec
      action_monitor_rate: 20

    scaled_vel_controller:
      type: velocity_controllers/ScaledJointTrajectoryController
      joints: *robot_joints
      constraints:
        # if zero, the executing duration would be decided by the start and end time of planned trajectory
        # set it big enough especially for position controller
        goal_time: 1.0 # 0.5 sec
        stopped_velocity_tolerance: 0.05 # 0.01 rad/s
        joint_1: { trajectory: 0.2, goal: 0.1 }
        joint_2: { trajectory: 0.2, goal: 0.1 }
        joint_3: { trajectory: 0.2, goal: 0.1 }
        joint_4: { trajectory: 0.2, goal: 0.1 }
        joint_5: { trajectory: 0.2, goal: 0.1 }
        joint_6: { trajectory: 0.2, goal: 0.1 }
      gains: # Required if controlling a velocity interface
        joint_1: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_2: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_3: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_4: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_5: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_6: {p: 100,  d: 1, i: 1, i_clamp: 1}
      # when starting the controller or canceling a trajectory, position hold mode is entered
      # this parameter specifies the time it takes to bring the current state (position and velocity) to a stop
      # if zero, the controller stops at the actual position
      stop_trajectory_duration: 0.0 # sec
      action_monitor_rate: 20

    pos_vel_controllers:
      type: pos_vel_controllers/JointTrajectoryController
      joints: *robot_joints
      constraints:
        # if zero, the executing duration would be decided by the start and end time of planned trajectory
        # set it big enough especially for position controller
        goal_time: 1.0 # 0.5 sec
        stopped_velocity_tolerance: 0.05 # 0.01 rad/s
        joint_1: { trajectory: 0.2, goal: 0.1 }
        joint_2: { trajectory: 0.2, goal: 0.1 }
        joint_3: { trajectory: 0.2, goal: 0.1 }
        joint_4: { trajectory: 0.2, goal: 0.1 }
        joint_5: { trajectory: 0.2, goal: 0.1 }
        joint_6: { trajectory: 0.2, goal: 0.1 }
      gains: # Required if controlling a velocity interface
        joint_1: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_2: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_3: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_4: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_5: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_6: {p: 100,  d: 1, i: 1, i_clamp: 1}
      # when starting the controller or canceling a trajectory, position hold mode is entered
      # this parameter specifies the time it takes to bring the current state (position and velocity) to a stop
      # if zero, the controller stops at the actual position
      stop_trajectory_duration: 0.0 # sec
      action_monitor_rate: 20

    pos_vel_acc_controllers:
      type: pos_vel_controllers/JointTrajectoryController
      joints: *robot_joints
      constraints:
        # if zero, the executing duration would be decided by the start and end time of planned trajectory
        # set it big enough especially for position controller
        goal_time: 1.0 # 0.5 sec
        stopped_velocity_tolerance: 0.05 # 0.01 rad/s
        joint_1: { trajectory: 0.2, goal: 0.1 }
        joint_2: { trajectory: 0.2, goal: 0.1 }
        joint_3: { trajectory: 0.2, goal: 0.1 }
        joint_4: { trajectory: 0.2, goal: 0.1 }
        joint_5: { trajectory: 0.2, goal: 0.1 }
        joint_6: { trajectory: 0.2, goal: 0.1 }
      gains: # Required if controlling a velocity interface
        joint_1: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_2: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_3: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_4: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_5: {p: 100,  d: 1, i: 1, i_clamp: 1}
        joint_6: {p: 100,  d: 1, i: 1, i_clamp: 1}
      # when starting the controller or canceling a trajectory, position hold mode is entered
      # this parameter specifies the time it takes to bring the current state (position and velocity) to a stop
      # if zero, the controller stops at the actual position
      stop_trajectory_duration: 0.0 # sec
      action_monitor_rate: 20
